{
  "name": "ranvier-monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "start": "bun run --filter 'ranviermudts' start",
    "dev": "bun run --filter 'ranviermudts' dev",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "packages/*"
  ]
}